<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../components/head.ejs') %>
    <title>editiorial of the todo</title>
  </head>
  <body>
    <%- include('../components/header.ejs') %>
    <h1 class="page-title">Edit your todo</h1>
    <div class="container">
      <form class="todo-card-edit" action="/update-todo?_method=PUT" method="post">
        <div class="line">
          <input type="hidden" name="id" value="<%= todo.id%>" />
          <input type="text" name="title" id="" value="<%= todo.title%>" />
          <input type="checkbox" name="isDone" checked="<%= todo.isDone%>" />
        </div>
        <input type="submit" value="submit your changes on the todo" />
      </form>
    </div>
  </body>
</html>
